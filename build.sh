#!/bin/bash
# Create models directory if it doesn't exist
mkdir -p models

# If you're including your model files in your repository,
# you don't need to download them, they'll already be there

# Install Python dependencies
pip install -r requirements.txt
